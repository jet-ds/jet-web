---
export interface Props {
  class?: string;
  clip?: boolean;
  padding?: 'none' | 'sm' | 'md' | 'lg';
  radius?: 'lg' | 'xl';
  surface?: 'base' | 'subtle';
  hover?: boolean;
}

const {
  class: className = '',
  clip = false,
  padding,
  radius,
  surface = 'base',
  hover = false,
  ...rest
} = Astro.props;

const baseClasses = 'border border-border-default transition-colors';

const surfaceClasses = {
  base: 'bg-surface-base',
  subtle: 'bg-bg-subtle',
};

const resolvedPadding = padding ?? (surface === 'subtle' ? 'lg' : 'md');
const resolvedRadius = radius ?? (surface === 'subtle' ? 'xl' : 'lg');

const radiusClasses = {
  lg: 'rounded-lg',
  xl: 'rounded-xl',
};

const paddingClasses = {
  none: '',
  sm: 'p-s',
  md: 'p-m',
  lg: 'p-card',
};

const hoverClasses = hover
  ? 'hover:border-brand-base hover:shadow-md cursor-pointer'
  : '';
const clipClass = clip ? 'overflow-hidden' : '';
const clippedFocusClass = clip && hover
  ? 'has-[:focus-visible]:outline has-[:focus-visible]:outline-2 has-[:focus-visible]:outline-offset-2 has-[:focus-visible]:outline-brand-base'
  : '';

const classes = `${surfaceClasses[surface]} ${radiusClasses[resolvedRadius]} ${baseClasses} ${paddingClasses[resolvedPadding]} ${hoverClasses} ${clipClass} ${clippedFocusClass} ${className}`;
---

<div class={classes} {...rest}>
  <slot />
</div>
