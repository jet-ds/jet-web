---
import type { CollectionDisplayRecord } from '../../features/collections/types';
import ContentCard from '../content/ContentCard.astro';

interface Props {
  record: CollectionDisplayRecord;
  variant?: 'collection' | 'compact';
}

const { record, variant = 'collection' } = Astro.props;
const tags = record.search?.tags ?? [];
---

<div class="h-full" data-filter-item data-filter-values={JSON.stringify(tags)}>
  <ContentCard
    href={record.href}
    title={record.title}
    summary={record.summary}
    image={record.image}
    facts={record.facts}
    variant={variant}
    headingLevel={variant === 'compact' ? 3 : 2}
  />
</div>
